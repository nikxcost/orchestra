# 🔒 Security Upgrade Guide

## Что было сделано

Добавлены критически важные меры безопасности:

1. ✅ **API Key аутентификация** - защита всех endpoints
2. ✅ **CORS конфигурация** - ограничение разрешенных origins
3. ✅ **Обратная совместимость** - работает без ключа для localhost разработки

---

## 🚀 Быстрый старт (для существующих пользователей)

### Шаг 1: Сгенерируйте API ключ

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

Скопируйте сгенерированный ключ.

### Шаг 2: Обновите backend/.env

Добавьте в `backend/.env`:

```env
# Ваш сгенерированный ключ
API_KEY=ваш_сгенерированный_ключ_здесь

# CORS origins (по умолчанию для localhost разработки)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
```

### Шаг 3: Создайте .env в корне проекта

Создайте файл `.env` в корневой папке (где `package.json`):

```env
VITE_API_BASE_URL=http://localhost:8000
VITE_API_KEY=ваш_сгенерированный_ключ_здесь
```

⚠️ **ВАЖНО:** `VITE_API_KEY` должен совпадать с `API_KEY` из backend!

### Шаг 4: Перезапустите сервисы

```bash
# Остановите backend (Ctrl+C)
# Перезапустите:
cd backend
python main.py

# В другом терминале остановите frontend (Ctrl+C)
# Перезапустите:
npm run dev
```

### Шаг 5: Проверка

При запуске backend вы должны увидеть:

```
✅ API_KEY configured successfully
```

Если видите предупреждение:

```
⚠️  API_KEY not set! API endpoints are unprotected.
```

Значит `.env` файл не загружен или `API_KEY` пустой.

---

## 📋 Что изменилось в коде

### Backend (main.py)

- ✅ Добавлен `verify_api_key()` - функция проверки API ключа
- ✅ Все endpoints (кроме `/health`) теперь требуют `X-API-Key` header
- ✅ CORS ограничен конкретными origins из `ALLOWED_ORIGINS`
- ✅ Обратная совместимость: если `API_KEY` не установлен, работает без аутентификации

### Frontend (src/services/api.ts)

- ✅ Добавлена функция `getHeaders()` - автоматически добавляет `X-API-Key`
- ✅ Все API вызовы используют правильные headers
- ✅ Поддержка `VITE_API_KEY` из environment variables

---

## 🔧 Режимы работы

### Режим разработки (без ключа)

Если `API_KEY` **не установлен**:

- ⚠️ Backend работает **без аутентификации**
- ⚠️ Выводится предупреждение в консоль
- ✅ Удобно для локальной разработки
- ❌ **НЕ ИСПОЛЬЗОВАТЬ в production!**

### Режим production (с ключом)

Если `API_KEY` **установлен**:

- ✅ Все endpoints защищены
- ✅ Требуется `X-API-Key` header
- ✅ Запросы без ключа получают 401 Unauthorized
- ✅ Безопасно для публичного доступа

---

## 🌐 Production Deployment

### Обновите переменные окружения

```env
# backend/.env
API_KEY=очень_длинный_случайный_ключ_минимум_32_символа
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
```

```env
# .env (frontend)
VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_KEY=тот_же_ключ_что_и_в_backend
```

### Обновите nginx конфигурацию (если используется)

Если вы используете nginx как reverse proxy, добавьте proxy для headers:

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:8000/;
    proxy_set_header X-API-Key $http_x_api_key;  # Проксировать API key
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
```

---

## 🧪 Тестирование

### Тест 1: Health endpoint (без аутентификации)

```bash
curl http://localhost:8000/health
```

**Ожидается:**
```json
{"status": "healthy"}
```

### Тест 2: Agents endpoint БЕЗ ключа (должен вернуть 401)

```bash
curl http://localhost:8000/agents
```

**Ожидается:**
```json
{"detail": "Missing API key. Provide X-API-Key header."}
```

### Тест 3: Agents endpoint С ключом (должен работать)

```bash
curl -H "X-API-Key: ваш_ключ_здесь" http://localhost:8000/agents
```

**Ожидается:**
```json
[
  {
    "id": "agent1",
    "name": "Агент вопросов",
    ...
  },
  ...
]
```

### Тест 4: Frontend

1. Откройте http://localhost:5173
2. Попробуйте отправить запрос
3. Проверьте в DevTools → Network → Headers должен быть `X-API-Key: ваш_ключ`

---

## ❓ Troubleshooting

### Проблема: "Missing API key"

**Причина:** Frontend не отправляет API ключ

**Решение:**
1. Проверьте `.env` файл в корне проекта
2. Убедитесь что `VITE_API_KEY` установлен
3. Перезапустите `npm run dev` (Vite не подхватывает изменения .env на лету)

### Проблема: "Invalid API key"

**Причина:** Ключи в backend и frontend не совпадают

**Решение:**
1. Сравните `API_KEY` в `backend/.env`
2. Сравните `VITE_API_KEY` в `.env`
3. Убедитесь что они **идентичны**
4. Перезапустите оба сервиса

### Проблема: CORS ошибка

**Причина:** Frontend origin не в списке `ALLOWED_ORIGINS`

**Решение:**
1. Проверьте `ALLOWED_ORIGINS` в `backend/.env`
2. Добавьте ваш frontend URL (например `http://localhost:5173`)
3. Перезапустите backend

### Проблема: "API_KEY not set" warning

**Причина:** Backend не видит `.env` файл

**Решение:**
1. Убедитесь что файл называется `.env` (не `.env.txt`)
2. Убедитесь что файл в папке `backend/`
3. Проверьте что `API_KEY=...` написан без пробелов вокруг `=`

---

## 🔐 Best Practices

### ✅ DO:
- Используйте криптографически стойкие ключи (минимум 32 символа)
- Генерируйте ключи с помощью `secrets.token_urlsafe()`
- Храните ключи в `.env` файлах (НЕ в коде)
- Используйте разные ключи для dev/staging/production
- Добавьте `.env` в `.gitignore` (уже сделано)

### ❌ DON'T:
- Не коммитьте `.env` файлы в git
- Не используйте простые пароли типа "password123"
- Не деплойте в production без API_KEY
- Не расшаривайте ключи публично
- Не используйте один ключ для всех окружений

---

## 📚 Дополнительная информация

- [README.md](README.md) - Полная документация проекта
- [DEPLOYMENT.md](DEPLOYMENT.md) - Инструкции по развертыванию
- [backend/README.md](backend/README.md) - Backend документация

---

**Вопросы?** Создайте issue в репозитории.

**Обновлено:** 2025-12-25
