# Руководство по редактированию агентов

## ✨ Новая функция: Редактирование агентов через UI

Теперь вы можете редактировать название, описание, цвет и системный промпт агентов прямо из интерфейса без изменения кода!

## 🚀 Как использовать

### 1. Запустите backend

```bash
cd backend
python3 main.py
```

При первом запуске создастся файл `backend/agents_config.json` с дефолтной конфигурацией 5 агентов.

### 2. Запустите frontend

```bash
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173)

### 3. Редактирование агента

1. На главном экране вы увидите карточки всех агентов
2. Наведите на карточку агента - в правом верхнем углу появится иконка карандаша ✏️
3. Нажмите на иконку - откроется модальное окно редактирования
4. Измените:
   - **Название** - отображается на карточке
   - **Описание** - краткое описание под названием
   - **Цвет** - выберите из 8 предустановленных цветов
   - **Системный промпт** - полный промпт агента (большое текстовое поле)
5. Нажмите **Сохранить**

### 4. Проверка изменений

- ✅ Изменения сохраняются в `backend/agents_config.json`
- ✅ Карточка агента обновляется мгновенно
- ✅ Промпт применяется при следующем запросе (без перезапуска backend!)

## 📁 Архитектура

### Backend

**Новые файлы:**
- `backend/agents_storage.py` - слой персистентности агентов
- `backend/agents_config.json` - конфигурация агентов (генерируется автоматически)
- `backend/.gitignore` - исключает `__pycache__` и `agents_config.json` из git

**Изменения:**
- `backend/main.py` - добавлены endpoints:
  - `GET /agents` - список всех агентов
  - `GET /agents/{id}` - получить агента по ID (с промптом)
  - `PUT /agents/{id}` - обновить агента
- `backend/orchestrator.py` - динамическая загрузка промптов из хранилища

### Frontend

**Новые файлы:**
- `src/components/AgentEditModal.tsx` - модальное окно редактирования

**Изменения:**
- `src/components/AgentCard.tsx` - добавлена кнопка редактирования
- `src/App.tsx` - интеграция модального окна, загрузка агентов из API
- `src/services/api.ts` - методы для работы с агентами

## 🎯 Примеры использования

### Кастомизация agent2

Допустим, вы хотите сделать из agent2 специалиста по SQL:

1. Нажмите ✏️ на карточке "Агент требований"
2. Измените:
   - **Название**: SQL Эксперт
   - **Описание**: Специалист по написанию и оптимизации SQL запросов
   - **Цвет**: Фиолетовый
   - **Промпт**:
     ```
     Вы эксперт по SQL и базам данных.
     Ваша задача - помогать писать, оптимизировать и объяснять SQL запросы.
     Всегда:
     - Используйте best practices
     - Объясняйте логику запроса
     - Предлагайте оптимизации
     - Указывайте на потенциальные проблемы
     ```
3. Сохраните

Теперь при запросе "Напиши запрос для выборки активных пользователей" оркестратор может выбрать вашего SQL эксперта!

### Создание специализированного промпта

Для agent1 вы можете настроить более детальный промпт под вашу специфику (например, добавить примеры из вашего домена).

## ⚙️ API Endpoints

### GET /agents
Возвращает список всех агентов (без промптов).

**Response:**
```json
[
  {
    "id": "agent1",
    "name": "Агент вопросов",
    "description": "Генерация уточняющих вопросов",
    "color": "bg-blue-500",
    "created_at": "2025-12-25T13:55:49.898492",
    "updated_at": "2025-12-25T13:55:49.898504"
  }
]
```

### GET /agents/{agent_id}
Возвращает полные данные агента включая промпт.

**Response:**
```json
{
  "id": "agent1",
  "name": "Агент вопросов",
  "description": "Генерация уточняющих вопросов",
  "prompt": "Ты — помощник бизнес-аналитика...",
  "color": "bg-blue-500",
  "created_at": "...",
  "updated_at": "..."
}
```

### PUT /agents/{agent_id}
Обновляет агента.

**Request:**
```json
{
  "name": "Новое название",
  "description": "Новое описание",
  "prompt": "Новый промпт",
  "color": "bg-purple-500"
}
```

## 🔧 Технические детали

### Динамическая загрузка промптов

При каждом запросе агент загружает актуальный промпт из `agents_config.json`:

```python
def mini_agent_node(agent_name: str):
    def node_function(state: AgentState) -> AgentState:
        # Обновляем промпты из хранилища при каждом запросе
        current_prompts = get_mini_agents_prompts()
        system_prompt = current_prompts[agent_name]
        # ...
```

Это позволяет обновлять промпты без перезапуска сервера!

### Персистентность

Все изменения сохраняются в `backend/agents_config.json`:

```json
{
  "agent1": {
    "id": "agent1",
    "name": "Агент вопросов",
    "description": "...",
    "prompt": "...",
    "color": "bg-blue-500",
    "created_at": "2025-12-25T13:55:49.898492",
    "updated_at": "2025-12-25T13:55:49.898504"
  }
}
```

## 🎨 Доступные цвета

- `bg-blue-500` - Синий
- `bg-green-500` - Зелёный
- `bg-yellow-500` - Жёлтый
- `bg-orange-500` - Оранжевый
- `bg-red-500` - Красный
- `bg-purple-500` - Фиолетовый
- `bg-pink-500` - Розовый
- `bg-indigo-500` - Индиго

## 🐛 Troubleshooting

### Изменения не применяются

1. Проверьте, что backend запущен
2. Проверьте консоль браузера на ошибки
3. Проверьте, что файл `backend/agents_config.json` обновился

### Backend не стартует

```bash
cd backend
pip install -r requirements.txt
python3 main.py
```

### Агенты не загружаются

Проверьте, что backend доступен на http://localhost:8000:
```bash
curl http://localhost:8000/agents
```

## 🚧 Ограничения текущей версии

- ❌ Нельзя создавать новых агентов (только редактировать существующие 5)
- ❌ Нельзя удалять агентов
- ❌ Нельзя менять граф выполнения
- ✅ Можно полностью кастомизировать поведение через промпт

## 📝 Следующие шаги

Возможные улучшения:
1. Добавление новых агентов через UI
2. Удаление агентов
3. Визуальный редактор графов
4. Импорт/экспорт конфигурации
5. Версионирование промптов
6. A/B тестирование разных промптов

---

**Создано в ветке:** `feature/agent-editor`

**Для возврата к основной версии:**
```bash
git checkout main
```
