# 🚀 Быстрая инструкция по развертыванию

## Проблема
На телефоне показывается ошибка "Backend не запущен", хотя на компьютере все работает.

## Причина
Браузер на телефоне кэширует старую версию JavaScript, которая обращается к `localhost:8000` вместо правильного адреса сервера.

---

## ⚡ Решение (3 шага)

### 1. Запушить изменения
```bash
git push origin main
```

### 2. Зайти на сервер и запустить деплой
```bash
ssh root@109.69.22.56
cd /root/orchestra
./deploy.sh
```

### 3. Проверить развертывание
```bash
./verify-deployment.sh
```

---

## 📱 На телефоне

### Очистить кэш браузера:

**iPhone:**
Settings → Safari → Clear History and Website Data

**Android:**
Settings → Privacy → Clear browsing data

### Или открыть в приватном режиме:
- Safari: Иконка вкладок → Private
- Chrome: Меню → New Incognito Tab

### Затем открыть:
http://109.69.22.56

---

## ✅ Должно работать!

Индикатор в левом нижнем углу должен показывать "Онлайн" (зелёный кружок).

---

## 🆘 Если не помогло

1. Убедитесь, что на сервере запущен `./deploy.sh`
2. Проверьте, что `./verify-deployment.sh` показывает все зелёные галочки
3. Попробуйте другой браузер на телефоне
4. Проверьте, что телефон подключён к интернету
5. Смотрите полную инструкцию в [MOBILE_FIX_GUIDE.md](MOBILE_FIX_GUIDE.md)
