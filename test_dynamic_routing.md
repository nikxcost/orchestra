# Тест динамической маршрутизации

## Что изменилось

Теперь оркестратор **динамически загружает** описания агентов из `agents_config.json` при каждом запросе!

## До изменений

```python
# Захардкожено в коде orchestrator.py:
routing_prompt = f"""...
- agent1: Генерация уточняющих вопросов для стейкхолдеров (...)
- agent2: Сбор и анализ требований
- agent3: Техническая документация
...
```

**Проблема:** Если вы меняете название/описание агента в UI, оркестратор всё равно видит старое описание.

## После изменений

```python
# Динамически из agents_config.json:
storage = get_storage()
agents = storage.get_all()

agents_list = "\n".join([
    f"- {agent['id']}: {agent['name']} - {agent['description']}"
    for agent in agents
])

routing_prompt = f"""...
{agents_list}
...
```

**Результат:** Оркестратор видит актуальные описания из `agents_config.json`!

## Пример теста

### 1. Исходное состояние

```bash
# Проверяем agent2
curl -s http://localhost:8000/agents/agent2 | python3 -m json.tool | grep -E '"name"|"description"'
```

Вывод:
```json
"name": "Агент требований",
"description": "Специалист по сбору и анализу требований",
```

### 2. Меняем agent2 через UI

1. Откройте http://localhost:5173
2. Нажмите ✏️ на карточке "Агент требований"
3. Измените:
   - **Название:** SQL Эксперт
   - **Описание:** Специалист по написанию и оптимизации SQL запросов
4. Сохраните

### 3. Проверяем, что изменилось

```bash
curl -s http://localhost:8000/agents/agent2 | python3 -m json.tool | grep -E '"name"|"description"'
```

Теперь вывод:
```json
"name": "SQL Эксперт",
"description": "Специалист по написанию и оптимизации SQL запросов",
```

### 4. Тестируем маршрутизацию

Отправьте запрос: **"Напиши SQL запрос для выборки активных пользователей"**

**До изменений:** Оркестратор мог выбрать agent1 или agent5, т.к. описание agent2 было "Сбор и анализ требований"

**После изменений:** Оркестратор выберет agent2, потому что видит актуальное описание "SQL Эксперт - Специалист по написанию и оптимизации SQL запросов"

## Как это работает

### route_question() теперь:

```python
def route_question(state: AgentState) -> AgentState:
    # 1. Загружаем агентов из хранилища
    storage = get_storage()
    agents = storage.get_all()

    # 2. Формируем список для промпта
    agents_list = "\n".join([
        f"- {agent['id']}: {agent['name']} - {agent['description']}"
        for agent in agents
    ])

    # 3. Оркестратор видит актуальные данные
    routing_prompt = f"""...
    {agents_list}
    ...
    """
```

### Валидация:

```python
# Если LLM вернет несуществующий agent_id:
if route not in current_prompts:
    # Fallback на первого доступного агента
    route = list(current_prompts.keys())[0]
```

## Полная синхронизация

Теперь **ВСЁ** синхронизировано с `agents_config.json`:

| Компонент | Источник данных | Обновляется динамически? |
|-----------|----------------|--------------------------|
| Frontend карточки | API /agents | ✅ Да |
| Промпт агента | agents_config.json | ✅ Да |
| Оркестратор routing | agents_config.json | ✅ Да |
| Логи | agents_config.json | ✅ Да |

## Преимущества

1. ✅ **Полная кастомизация через UI** - всё меняется одновременно
2. ✅ **Консистентность** - нет расхождений между кодом и конфигом
3. ✅ **Гибкость** - можно экспериментировать с описаниями для лучшей маршрутизации
4. ✅ **A/B тестирование** - легко тестировать разные формулировки

## Итог

Теперь система **полностью динамическая**! Когда вы меняете агента в UI:

1. Обновляется `agents_config.json`
2. Frontend сразу показывает новое название/описание
3. Оркестратор при следующем запросе использует новое описание для маршрутизации
4. Агент использует новый промпт при выполнении

**Никакого перезапуска не требуется!** 🎉
