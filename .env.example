# Frontend Configuration

# API Base URL
# Development: http://localhost:8000
# Production: http://your-server-ip/api (через nginx proxy)
# Example: http://109.69.22.56/api
VITE_API_BASE_URL=http://localhost:8000

# API Key (optional, only if backend requires authentication)
# Must match the API_KEY in backend/.env
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Leave empty for local development without authentication
VITE_API_KEY=
